var coinChange = (n, c, cache) => {
  var cacheTotal = 0

  if (cache.length) {
    for (let x = 0; x < cache.length; x++) {
      cacheTotal += cache[x]
    }
  }
  
  if (n = 0) return cache.length
  else {
    if ()
  }
}

coinChange(10, [2, 5, 3, 6], [])